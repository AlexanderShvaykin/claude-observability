Рабочий каталог для poll-limits.sh.

`.claude/settings.json` здесь выключает телеметрию: project-level настройки перебивают
пользовательские, поэтому опрос `/usage` не создаёт лишнюю «сессию» в метриках.
Переменными окружения так сделать нельзя — env из ~/.claude/settings.json их перебивает.
